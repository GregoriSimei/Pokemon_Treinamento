pipeline
{

    agent any

    stages 
    {

        stage('Build Angular Project')
        {
            steps 
            {
                sh 'npm install'
                sh 'ng build --prod'
            }
        }

        stage('Deployment a Zip')
        {
            steps
            {
                sh 'echo oi'
            }
        }
    }
}