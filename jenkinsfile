pipeline
{
    agent any

    stages 
    {
        stage('Build Angular Project')
        {
            steps 
            {
                sh 'npm install'
                sh 'ng build --prod'
            }
        }

        stage('Deployment')
        {
            steps
            {
                sh 'echo oi'
            }
        }
    }
}