 pipeline{
    
    agent { Label 'ubuntu_server'}

    stages {
        
        stage('Build Angular Project'){
            
            steps {
                sh 'npm install'
                sh 'ng build --prod'
            }
        }

        stage('Deployment'){
            
            steps{
                sh 'echo oi'
            }
        }

    }

}