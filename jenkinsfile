 pipeline{
    
    agent any

    stages {

	stage('Angular tests'){
	   steps{
	   	sh 'ls -l'
	   }
	}

        stage('Build Angular Project'){
	    agent { dockerfile true }
            steps {
		            sh 'ng build --prod'
            }
        }

        stage('Deployment'){
             steps{
                sh 'pwd'
            }
        }

    }

}
